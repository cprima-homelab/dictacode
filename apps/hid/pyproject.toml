[project]
name = "dictacode-hid"
version = "0.1.0"
description = "dictacode Zero HID bridge (stub)"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
dictacode-hid = "dictacode_hid.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dictacode_hid"]
