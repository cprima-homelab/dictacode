# dictacode Keymap Configuration
#
# Specifies the keyboard layout to use for HID output.
# Available keymaps: en_us, de_de
#
# Change this and restart the HID service:
#   sudo systemctl restart dictacode-hid

keymap=en_us
